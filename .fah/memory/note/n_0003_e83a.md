---
id: "n_0003_e83a"
type: "note"
title: "dmtools-agents item Б DONE (branch feat/tracker-agnostic-helpers @ d9fc8fc, pushed, not merged): js/common/trackers.js — tracker-agnostic ticket layer, the scm.js analog over the tracker_* tool family (get_ticket/search/post_comment/get_comments/add_label/remove_label/move_to_status/assign_to/create_ticket). Per-operation probing with transparent jira_* fallback for runtimes without the family; normalizeTicket() flattens Jira/GitHub/ADO payloads; bare issue numbers expand to owner/repo#N only when repository is configured; assignForReview() keeps jiraHelpers semantics without touching jiraHelpers.js. TDD: js/unit-tests/test_trackers.js (36 tests, node harness + QuickJS via run_all.json + standalone run_trackers.json). Suite baseline after change: 904 passed / 14 failed (same known clusters). jiraHelpers.js and all existing suites untouched."
author: "agent"
date: "2026-09-07T17:12:48.976241Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "dmtools-agents", "trackers", "scm-abstraction", "tdd", "branch", "feature"]
---


# Note: n_0003_e83a

dmtools-agents item Б DONE (branch feat/tracker-agnostic-helpers @ d9fc8fc, pushed, not merged): js/common/trackers.js — tracker-agnostic ticket layer, the scm.js analog over the tracker_* tool family (get_ticket/search/post_comment/get_comments/add_label/remove_label/move_to_status/assign_to/create_ticket). Per-operation probing with transparent jira_* fallback for runtimes without the family; normalizeTicket() flattens Jira/GitHub/ADO payloads; bare issue numbers expand to owner/repo#N only when repository is configured; assignForReview() keeps jiraHelpers semantics without touching jiraHelpers.js. TDD: js/unit-tests/test_trackers.js (36 tests, node harness + QuickJS via run_all.json + standalone run_trackers.json). Suite baseline after change: 904 passed / 14 failed (same known clusters). jiraHelpers.js and all existing suites untouched.

**By:** [[agent]]
**Date:** 2026-09-07T17:12:48.976241Z
**Area:** [[project|project]]
