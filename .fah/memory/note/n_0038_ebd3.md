---
id: "n_0038_ebd3"
type: "note"
title: "dmtools-dart `cli_execute_command` JS bridge (tool_bridge.dart) now matches Java CliCommandExecutor parity: full command line with first-token whitelist (12 base commands + CLI_ALLOWED_COMMANDS override), temp shell script via /bin/sh (cmd /c on Windows), combined stdout+stderr, plain-string trimmed output, workingDirectory with git-root resolution and allowed-base validation, env injection (GIT_PAGER=cat, GIT_TERMINAL_PROMPT=0, PATH expansion, dmtools.env, job overrides); Dart-only `cli_execute_command_with_env` stays on legacy path."
author: "agent"
date: "2026-09-07T22:00:34.466660Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.75
tags: ["#note", "#source_agent", "java-parity", "contract"]
---


# Note: n_0038_ebd3

dmtools-dart `cli_execute_command` JS bridge (tool_bridge.dart) now matches Java CliCommandExecutor parity: full command line with first-token whitelist (12 base commands + CLI_ALLOWED_COMMANDS override), temp shell script via /bin/sh (cmd /c on Windows), combined stdout+stderr, plain-string trimmed output, workingDirectory with git-root resolution and allowed-base validation, env injection (GIT_PAGER=cat, GIT_TERMINAL_PROMPT=0, PATH expansion, dmtools.env, job overrides); Dart-only `cli_execute_command_with_env` stays on legacy path.

**By:** [[agent]]
**Date:** 2026-09-07T22:00:34.466660Z
**Area:** [[project|project]]
