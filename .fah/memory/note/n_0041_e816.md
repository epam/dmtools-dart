---
id: "n_0041_e816"
type: "note"
title: "Agents suite (L4) is FULLY GREEN and blocking again (2026-09-07, session 4): agents/ submodule re-pointed to 432cd4c (merged dmtools-agents PR #407, upstream fixed the 14 formerly-failing tests), `dart run bin/run_agents_suite.dart agents` → 882 passed / 0 failed, and the continue-on-error fuse was removed from quality.yml — the suite job is a hard gate. Baseline to watch: any suite failure is now a regression. The Teammate job is ported (lib/src/agents/teammate_job.dart, dispatched via AgentFactory 'teammate'): inputJql → Jira sync tools (search/get_ticket/get_comments hydration) → per-ticket CliAgent with contextId = ticket key; trace comment parity (AbstractJob.agentNamePrefix/shouldPostComments). Remaining fuse: merge-trigger.yml is still off via the MERGE_TRIGGER_ENABLED repo variable — the 'until Teammate is ported' blocker is gone, flipping it on is a separate infra decision."
author: "agent"
date: "2026-09-07T22:25:59.663858Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "agents-suite", "ci", "baseline", "teammate", "resolved"]
---


# Note: n_0041_e816

Agents suite (L4) is FULLY GREEN and blocking again (2026-09-07, session 4): agents/ submodule re-pointed to 432cd4c (merged dmtools-agents PR #407, upstream fixed the 14 formerly-failing tests), `dart run bin/run_agents_suite.dart agents` → 882 passed / 0 failed, and the continue-on-error fuse was removed from quality.yml — the suite job is a hard gate. Baseline to watch: any suite failure is now a regression. The Teammate job is ported (lib/src/agents/teammate_job.dart, dispatched via AgentFactory 'teammate'): inputJql → Jira sync tools (search/get_ticket/get_comments hydration) → per-ticket CliAgent with contextId = ticket key; trace comment parity (AbstractJob.agentNamePrefix/shouldPostComments). Remaining fuse: merge-trigger.yml is still off via the MERGE_TRIGGER_ENABLED repo variable — the 'until Teammate is ported' blocker is gone, flipping it on is a separate infra decision.

**By:** [[agent]]
**Date:** 2026-09-07T22:25:59.663858Z
**Area:** [[project|project]]
