---
id: "n_0079_67e2"
type: "note"
title: "Prerequisites wiring in ai-teammate-issues.yml (dmtools-dart PR #18 @ 6e0a31d, owner: \"не вижу где ты ставишь fa\" + reference IstiN/dmtools ai-teammate.yml): workflow now installs agent CLIs up front, cached by version — FA_VERSION pinned (vars.FA_VERSION || 'v0.1.324', IstiN/flutter_agent_harness release), KIMI_VERSION (vars || empty = installer default); actions/cache@v6 for fa (~/.local/bin/fa + ~/lib, key fa-<ver>-<os>-<arch>) and kimi (~/.kimi-code via `agents/setup/cache.sh keys fa-session kimi` → KIMI_CACHE_PATH/KEY); install via `bash agents/setup/install.sh \"fa:${FA_VERSION}\" kimi` (idempotent no-op on cache hit); verify step fa/kimi --version. NO node: fa is Dart AOT, kimi self-contained (install.sh auto-injects node only for npm tools copilot/codemie/codegraph). register_path (setup/_common.sh) exports PATH immediately + persists GITHUB_PATH — no manual PATH edits. agents/setup/install.sh spec format tool:version (TOOL_NAME/TOOL_VERSION split on first ':'). agents/ is a SUBMODULE (dmtools-agents) — cache.sh has no plain `fa` entry (only fa-session); fa CLI cache keys are declared directly in the workflow."
author: "agent"
date: "2026-09-08T14:05:14.604027Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "dmtools-dart", "machine-kit", "prerequisites", "fa-install", "pr-18"]
---


# Note: n_0079_67e2

Prerequisites wiring in ai-teammate-issues.yml (dmtools-dart PR #18 @ 6e0a31d, owner: "не вижу где ты ставишь fa" + reference IstiN/dmtools ai-teammate.yml): workflow now installs agent CLIs up front, cached by version — FA_VERSION pinned (vars.FA_VERSION || 'v0.1.324', IstiN/flutter_agent_harness release), KIMI_VERSION (vars || empty = installer default); actions/cache@v6 for fa (~/.local/bin/fa + ~/lib, key fa-<ver>-<os>-<arch>) and kimi (~/.kimi-code via `agents/setup/cache.sh keys fa-session kimi` → KIMI_CACHE_PATH/KEY); install via `bash agents/setup/install.sh "fa:${FA_VERSION}" kimi` (idempotent no-op on cache hit); verify step fa/kimi --version. NO node: fa is Dart AOT, kimi self-contained (install.sh auto-injects node only for npm tools copilot/codemie/codegraph). register_path (setup/_common.sh) exports PATH immediately + persists GITHUB_PATH — no manual PATH edits. agents/setup/install.sh spec format tool:version (TOOL_NAME/TOOL_VERSION split on first ':'). agents/ is a SUBMODULE (dmtools-agents) — cache.sh has no plain `fa` entry (only fa-session); fa CLI cache keys are declared directly in the workflow.

**By:** [[agent]]
**Date:** 2026-09-08T14:05:14.604027Z
**Area:** [[project|project]]
