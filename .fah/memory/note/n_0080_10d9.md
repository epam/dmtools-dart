---
id: "n_0080_10d9"
type: "note"
title: "Prerequisites final shape in ai-teammate-issues.yml (dmtools-dart PR #18 @ 056f64f, owner corrections): kimi CLI REMOVED — fa is self-contained, all runners use AI_AGENT_PROVIDER=fa and the kimi backend is FA_PROVIDER_CONFIG inside fa (even fa-review-kimi.json); standalone kimi binary never invoked. Flutter added: subosito/flutter-action@v2 cache:true, FLUTTER_VERSION default 3.44.4 (dev machine version, vars override) — provides the Dart SDK. dmtools-dart runs FROM SOURCE in its own machine workflow: dart pub get + make native + `dart run bin/dmtools.dart run <runner>` — release install.sh binary LAGS the branch (PR #18 features unreleased), machine repo dogfoods HEAD; QuickJS bridge resolves via .dart_tool/package_config.json (no JSR_QUICKJS_LIB launcher needed for dart run). fa CLI install kept (agents/setup/install.sh \"fa:$FA_VERSION\", cache by version ~/.local/bin/fa + ~/lib). Local machine: flutter 3.44.4 user-branch at ~/.local/flutter."
author: "agent"
date: "2026-09-08T14:10:36.727788Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "dmtools-dart", "machine-kit", "flutter", "prerequisites", "pr-18"]
---


# Note: n_0080_10d9

Prerequisites final shape in ai-teammate-issues.yml (dmtools-dart PR #18 @ 056f64f, owner corrections): kimi CLI REMOVED — fa is self-contained, all runners use AI_AGENT_PROVIDER=fa and the kimi backend is FA_PROVIDER_CONFIG inside fa (even fa-review-kimi.json); standalone kimi binary never invoked. Flutter added: subosito/flutter-action@v2 cache:true, FLUTTER_VERSION default 3.44.4 (dev machine version, vars override) — provides the Dart SDK. dmtools-dart runs FROM SOURCE in its own machine workflow: dart pub get + make native + `dart run bin/dmtools.dart run <runner>` — release install.sh binary LAGS the branch (PR #18 features unreleased), machine repo dogfoods HEAD; QuickJS bridge resolves via .dart_tool/package_config.json (no JSR_QUICKJS_LIB launcher needed for dart run). fa CLI install kept (agents/setup/install.sh "fa:$FA_VERSION", cache by version ~/.local/bin/fa + ~/lib). Local machine: flutter 3.44.4 user-branch at ~/.local/flutter.

**By:** [[agent]]
**Date:** 2026-09-08T14:10:36.727788Z
**Area:** [[project|project]]
