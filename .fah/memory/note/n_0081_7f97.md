---
id: "n_0081_7f97"
type: "note"
title: "fa project memory is now repo content in dmtools-dart (PR #18 @ 5894d12, owner: \"fa сохраняет memory в репозиторий\"): fa stores project memory under <git-root>/.fah/memory (MEMORY.md, MEMORY.revision, GRAPH.md, DELETIONS.md, note/answer/question/*.md with frontmatter area:\"project\"); .gitignore changed from blanket .fah/ to `.fah/*` + `!.fah/memory/` (only .last_maintenance ignored inside — bash_jobs/logs/model_cache.json stay out); existing memory (6 notes, 324K) committed as bootstrap; ai-teammate-issues.yml gained an always() step after the agent run that git-commits+pushes .fah/memory (\"chore(fa): persist agent project memory [skip ci]\", best-effort — protected branch downgrades to ::warning:); next run's checkout brings accumulated memory back. CI gotcha hit: a push to the branch can silently produce ZERO workflow runs (7411360) — retrigger with an empty commit. flutter_agent_harness GitHub clone is slow (>5 min, 30MB+ pack) — inspect fa behavior empirically via ~/.fah/ layout and `fa --help` instead."
author: "agent"
date: "2026-09-08T15:39:04.513881Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "dmtools-dart", "machine-kit", "fa-memory", "pr-18"]
---


# Note: n_0081_7f97

fa project memory is now repo content in dmtools-dart (PR #18 @ 5894d12, owner: "fa сохраняет memory в репозиторий"): fa stores project memory under <git-root>/.fah/memory (MEMORY.md, MEMORY.revision, GRAPH.md, DELETIONS.md, note/answer/question/*.md with frontmatter area:"project"); .gitignore changed from blanket .fah/ to `.fah/*` + `!.fah/memory/` (only .last_maintenance ignored inside — bash_jobs/logs/model_cache.json stay out); existing memory (6 notes, 324K) committed as bootstrap; ai-teammate-issues.yml gained an always() step after the agent run that git-commits+pushes .fah/memory ("chore(fa): persist agent project memory [skip ci]", best-effort — protected branch downgrades to ::warning:); next run's checkout brings accumulated memory back. CI gotcha hit: a push to the branch can silently produce ZERO workflow runs (7411360) — retrigger with an empty commit. flutter_agent_harness GitHub clone is slow (>5 min, 30MB+ pack) — inspect fa behavior empirically via ~/.fah/ layout and `fa --help` instead.

**By:** [[agent]]
**Date:** 2026-09-08T15:39:04.513881Z
**Area:** [[project|project]]
