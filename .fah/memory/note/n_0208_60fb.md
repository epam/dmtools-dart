---
id: "n_0208_60fb"
type: "note"
title: "gh-125 fix shape (dmtools-dart): CliAgent passes trackerType = propertyReader.getDefaultTracker() into CliCommandBuilder.buildCommands (Java parity: buildCommands internally uses configuration.getDefaultTracker()). Dev+rework machine-kit runners pin params.envVariables.DEFAULT_TRACKER=github + cliPromptsByTracker.github → machine-kit/teammate-install/instructions/github_comment_format.md (machine-kit-local; parents in agents/ stay tracker-agnostic, review runner deliberately untouched). DELIBERATE parity gap remaining: Java's CliCommandBuilder.resolvePromptTrackerType (customParams.contentOutput.target==\"confluence\" overrides the tracker key when cliPromptsByTracker has a confluence entry) is NOT ported to Dart — tied to the unported InstructionProcessor/confluence-output work, not an oversight."
author: "agent"
date: "2026-09-15T20:03:32.588211Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "dmtools-dart", "gh-125", "cli-agent", "parity", "tracker-prompts", "machine-kit"]
---


# Note: n_0208_60fb

gh-125 fix shape (dmtools-dart): CliAgent passes trackerType = propertyReader.getDefaultTracker() into CliCommandBuilder.buildCommands (Java parity: buildCommands internally uses configuration.getDefaultTracker()). Dev+rework machine-kit runners pin params.envVariables.DEFAULT_TRACKER=github + cliPromptsByTracker.github → machine-kit/teammate-install/instructions/github_comment_format.md (machine-kit-local; parents in agents/ stay tracker-agnostic, review runner deliberately untouched). DELIBERATE parity gap remaining: Java's CliCommandBuilder.resolvePromptTrackerType (customParams.contentOutput.target=="confluence" overrides the tracker key when cliPromptsByTracker has a confluence entry) is NOT ported to Dart — tied to the unported InstructionProcessor/confluence-output work, not an oversight.

**By:** [[agent]]
**Date:** 2026-09-15T20:03:32.588211Z
**Area:** [[project|project]]
