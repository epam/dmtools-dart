---
id: "n_0209_bfd3"
type: "note"
title: "gh-125 rework (PR #127) added jiraMarkupToMarkdown (lib/src/js/sync_tools/jira_markup.dart) — the owner refined scope on the ticket: gh-122's {code}/h3. markup came from hard-coded Jira-wiki templates in dmtools-agents JS post-actions, NOT LLM output, so TrackerGitHubRouter._postComment now converts bodies before POSTing to GitHub Issues ({code:lang}/{code}lang/{code} blocks → fences, h1.-h6. → #, [text|url] → [text](url), *bold* → **bold** with lookaround guards, {panel:title=X} → blockquote, {color} stripped; inline code pairs embedded after text become backtick spans because a mid-line fence would not open a GFM block). Jira path passes bodies through unchanged (converter imported ONLY by the router). Still NOT ported (unchanged from dev run): Java CliCommandBuilder.resolvePromptTrackerType contentOutput=confluence routing."
author: "agent"
date: "2026-09-15T20:29:54.645273Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "dmtools-dart", "gh-125", "tracker-github-router", "jira-markup", "rework"]
---


# Note: n_0209_bfd3

gh-125 rework (PR #127) added jiraMarkupToMarkdown (lib/src/js/sync_tools/jira_markup.dart) — the owner refined scope on the ticket: gh-122's {code}/h3. markup came from hard-coded Jira-wiki templates in dmtools-agents JS post-actions, NOT LLM output, so TrackerGitHubRouter._postComment now converts bodies before POSTing to GitHub Issues ({code:lang}/{code}lang/{code} blocks → fences, h1.-h6. → #, [text|url] → [text](url), *bold* → **bold** with lookaround guards, {panel:title=X} → blockquote, {color} stripped; inline code pairs embedded after text become backtick spans because a mid-line fence would not open a GFM block). Jira path passes bodies through unchanged (converter imported ONLY by the router). Still NOT ported (unchanged from dev run): Java CliCommandBuilder.resolvePromptTrackerType contentOutput=confluence routing.

**By:** [[agent]]
**Date:** 2026-09-15T20:29:54.645273Z
**Area:** [[project|project]]
