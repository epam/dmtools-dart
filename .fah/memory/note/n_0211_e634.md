---
id: "n_0211_e634"
type: "note"
title: "gh-125 rework round 3 (PR #127): the automated REQUEST_CHANGES review found 1 BLOCKING + 1 IMPORTANT + 4 suggestions in jiraMarkupToMarkdown — all fixed. (1) Null-assert crash on whitespace after bare {code} tags (_langAndBody now matches afterTag.trimLeft() with null-safe fallback). (2) Markdown-fence blindness fixed via _Converter._inMdFence state (lines inside ``` fences pass verbatim; the reviewer's marker-gate option alone could NOT fix their own example since the fenced h3. matches the marker scan) PLUS a whole-body Jira-marker gate ({code,{panel,{color,{{,[x|y],^\s*h[1-6]\.) so marker-free bodies return byte-for-byte — which also preserves GFM *italic* (bold conversion is now Jira-sourced-bodies-only, and '*' deliberately NOT a marker or bullets would open the gate). (3) dead {panel} branch deleted, _panelOpen/_codeOpen whitespace-lenient. (4) single-line pair lang hint via letter-leading first token (only whole-line/fence path; 2 + 2 never becomes lang=2). (5) {{monospace}} → backtick span. Converter now 49 tests; suite 2774 passed."
author: "agent"
date: "2026-09-16T16:36:19.437274Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "dmtools-dart", "gh-125", "jira-markup", "rework", "resolved"]
---


# Note: n_0211_e634

gh-125 rework round 3 (PR #127): the automated REQUEST_CHANGES review found 1 BLOCKING + 1 IMPORTANT + 4 suggestions in jiraMarkupToMarkdown — all fixed. (1) Null-assert crash on whitespace after bare {code} tags (_langAndBody now matches afterTag.trimLeft() with null-safe fallback). (2) Markdown-fence blindness fixed via _Converter._inMdFence state (lines inside ``` fences pass verbatim; the reviewer's marker-gate option alone could NOT fix their own example since the fenced h3. matches the marker scan) PLUS a whole-body Jira-marker gate ({code,{panel,{color,{{,[x|y],^\s*h[1-6]\.) so marker-free bodies return byte-for-byte — which also preserves GFM *italic* (bold conversion is now Jira-sourced-bodies-only, and '*' deliberately NOT a marker or bullets would open the gate). (3) dead {panel} branch deleted, _panelOpen/_codeOpen whitespace-lenient. (4) single-line pair lang hint via letter-leading first token (only whole-line/fence path; 2 + 2 never becomes lang=2). (5) {{monospace}} → backtick span. Converter now 49 tests; suite 2774 passed.

**By:** [[agent]]
**Date:** 2026-09-16T16:36:19.437274Z
**Area:** [[project|project]]
