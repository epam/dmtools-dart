---
id: "n_0213_aa9c"
type: "note"
title: "Machine factory e2e PROVEN (2026-09-19): fa gh-623 went the full cycle assigned→dev→PR#646→review REQUEST_CHANGES→rework→approve→merge→closed-by-SM-tick, running through dmtools-agents reusable workflows (factory-teammate.yml/factory-sm.yml) pinned by SHA in target repos (dmtools-dart + fa). Key infra lessons: (1) agent's final `git add -A` captured the factory-agents checkout as a gitlink with no .gitmodules — factory now seeds factory-agents/ into .git/info/exclude before the agent runs; (2) job start/done task-list markers are posted by the factory itself (no tool dependency); (3) `gh run rerun` of old runs on a PR branch is useless — new pushes/reruns cancel each other, watch the newest run id; (4) fa PTY suite tests (shell_job_countdown_pty, header_badge_pty) flake on runners ~1/3 of runs — rerun the failed leg, do not blame the diff."
author: "agent"
date: "2026-09-19T00:20:34.568799Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "machine-factory", "e2e", "fa", "dmtools-agents"]
---


# Note: n_0213_aa9c

Machine factory e2e PROVEN (2026-09-19): fa gh-623 went the full cycle assigned→dev→PR#646→review REQUEST_CHANGES→rework→approve→merge→closed-by-SM-tick, running through dmtools-agents reusable workflows (factory-teammate.yml/factory-sm.yml) pinned by SHA in target repos (dmtools-dart + fa). Key infra lessons: (1) agent's final `git add -A` captured the factory-agents checkout as a gitlink with no .gitmodules — factory now seeds factory-agents/ into .git/info/exclude before the agent runs; (2) job start/done task-list markers are posted by the factory itself (no tool dependency); (3) `gh run rerun` of old runs on a PR branch is useless — new pushes/reruns cancel each other, watch the newest run id; (4) fa PTY suite tests (shell_job_countdown_pty, header_badge_pty) flake on runners ~1/3 of runs — rerun the failed leg, do not blame the diff.

**By:** [[agent]]
**Date:** 2026-09-19T00:20:34.568799Z
**Area:** [[project|project]]
