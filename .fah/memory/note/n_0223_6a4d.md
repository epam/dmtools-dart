---
id: "n_0223_6a4d"
type: "note"
title: "runAsync/wait IMPLEMENTED in dmtools-dart (PR #226 → issue #224, branch feat/runasync-parallel-js, commit ca36369): Option A engine-worker pool. Key files: lib/src/js/async_job_pool.dart (pool+workers), async_prelude.dart (JS preludes), engine_factory.dart (EngineSpec + wireEngine extracted from JsJobRunner._wireRuntime — main and worker engines share one wiring path). Knob: params.jobParams.parallelWorkers >= 2 wires runAsync; unset/1 = runAsync not wired (default-off). Pitfalls hit: (1) C bridge JS_ParseJSONs host results — JS receives REAL VALUES, never JSON.parse them again in the prelude; (2) direct file_read returns JS null for missing files (jsonEncode'd 'null' → parsed); (3) __setScriptDirectory expects a FILE path (derives dir) — workers anchor their dir with a virtual '__jsr_job__.js' name; (4) native_synchronization Mailbox.put THROWS StateError when full (capacity 1) — per-worker doneBox is safe only because main drains before re-dispatching; (5) worker isolates must dispose their own SyncHttpBridge in a finally (isolate hygiene for dart test); (6) Isolate.kill(beforeNextEvent) lets the worker unwind (finally runs), immediate kill leaks its HTTP child isolate and hangs dart test. Dart-side edit race lesson: two edit calls against the same file in one parallel tool block = lost update — always sequential. Gates at time of push: 3250 tests (3 pre-existing env failures on clean main: allowed_base /var symlink + github issue-tracker env leakage), coverage 96.7%, crap4dart 20 passed / Max CRAP 8.00 (EngineSpec max_params:10 exemption in crap4dart.yaml), agents suite 1096/0 (suite grew from 917 — submodule moved). CI dispatched (dispatch-only repo): Quality queued, SM Quality Gate in_progress on the branch."
author: "agent"
date: "2026-09-22T22:06:07.314605Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "note", "runasync", "parallel-js", "pr-226", "issue-224", "quickjs", "isolate", "pitfall", "dmtools-dart"]
---


# Note: n_0223_6a4d

runAsync/wait IMPLEMENTED in dmtools-dart (PR #226 → issue #224, branch feat/runasync-parallel-js, commit ca36369): Option A engine-worker pool. Key files: lib/src/js/async_job_pool.dart (pool+workers), async_prelude.dart (JS preludes), engine_factory.dart (EngineSpec + wireEngine extracted from JsJobRunner._wireRuntime — main and worker engines share one wiring path). Knob: params.jobParams.parallelWorkers >= 2 wires runAsync; unset/1 = runAsync not wired (default-off). Pitfalls hit: (1) C bridge JS_ParseJSONs host results — JS receives REAL VALUES, never JSON.parse them again in the prelude; (2) direct file_read returns JS null for missing files (jsonEncode'd 'null' → parsed); (3) __setScriptDirectory expects a FILE path (derives dir) — workers anchor their dir with a virtual '__jsr_job__.js' name; (4) native_synchronization Mailbox.put THROWS StateError when full (capacity 1) — per-worker doneBox is safe only because main drains before re-dispatching; (5) worker isolates must dispose their own SyncHttpBridge in a finally (isolate hygiene for dart test); (6) Isolate.kill(beforeNextEvent) lets the worker unwind (finally runs), immediate kill leaks its HTTP child isolate and hangs dart test. Dart-side edit race lesson: two edit calls against the same file in one parallel tool block = lost update — always sequential. Gates at time of push: 3250 tests (3 pre-existing env failures on clean main: allowed_base /var symlink + github issue-tracker env leakage), coverage 96.7%, crap4dart 20 passed / Max CRAP 8.00 (EngineSpec max_params:10 exemption in crap4dart.yaml), agents suite 1096/0 (suite grew from 917 — submodule moved). CI dispatched (dispatch-only repo): Quality queued, SM Quality Gate in_progress on the branch.

**By:** [[agent]]
**Date:** 2026-09-22T22:06:07.314605Z
**Area:** [[project|project]]
