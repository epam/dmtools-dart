---
id: "n_0229_cd9d"
type: "note"
title: "gh-242 fully closed (2026-09-26, branch ai/gh-242): the __jsr* protocol has ONE owner — quickjs_runtime 0.3.4 (IstiN/quickjs_runtime#15 per-runtime dispatchContext; jsr half) + PR #246 delegation (consumer half). This session landed the #246 approved-review follow-ups: pubspec lower bound bumped ^0.3.3→^0.3.4 (dispatchContext: only exists in 0.3.4 — keep lower bounds honest, review flags this pattern), regression test 'worker engines see overrides snapshotted at dispatch time' in test/js/async_job_pool_test.dart (set_env_variable after wiring + cli_execute_command on worker; proves dispatch-time PropertyReader snapshot via the whitelist+env path), dead re-exports trimmed to `show AsyncJobEnvelope`. The async_prelude_contract_test tripwire named in the ticket never landed on main (only on abandoned feat/runasync-parallel-js) — the new overrides test + conformance suite are the guards."
author: "agent"
date: "2026-09-26T18:15:27.877772Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "dmtools-dart", "gh-242", "quickjs_runtime", "async-job-pool", "pubspec", "review-followups"]
---


# Note: n_0229_cd9d

gh-242 fully closed (2026-09-26, branch ai/gh-242): the __jsr* protocol has ONE owner — quickjs_runtime 0.3.4 (IstiN/quickjs_runtime#15 per-runtime dispatchContext; jsr half) + PR #246 delegation (consumer half). This session landed the #246 approved-review follow-ups: pubspec lower bound bumped ^0.3.3→^0.3.4 (dispatchContext: only exists in 0.3.4 — keep lower bounds honest, review flags this pattern), regression test 'worker engines see overrides snapshotted at dispatch time' in test/js/async_job_pool_test.dart (set_env_variable after wiring + cli_execute_command on worker; proves dispatch-time PropertyReader snapshot via the whitelist+env path), dead re-exports trimmed to `show AsyncJobEnvelope`. The async_prelude_contract_test tripwire named in the ticket never landed on main (only on abandoned feat/runasync-parallel-js) — the new overrides test + conformance suite are the guards.

**By:** [[agent]]
**Date:** 2026-09-26T18:15:27.877772Z
**Area:** [[project|project]]
