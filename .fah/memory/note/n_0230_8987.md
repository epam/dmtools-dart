---
id: "n_0230_8987"
type: "note"
title: "gh-257 rework round complete (PR #259, 2026-09-26): the first fix (PR #258, already on main) and the consolidation in #259 collided — merging main back regressed `method_size` (`pathRewriteTests` 103 lines > max 60). Final shape: THREE group functions in test/pack/agent_pack_resolver_test.dart — pathRewriteTests (27 lines, basic rewrites), pathRewriteNestedTests (43, ./-prefix + nested list/map shapes), pathRewriteUntouchedTests (40, empty/plain-http/absolute/../-escape/URL/classpath pins) — all wired in main(); `_isNonPathRef` is `static`. Lesson: the 60-line method_size gate counts group-function bodies, so a 103-line function needs a 3-way split (a 2-way split of rewrite tests 1+2 alone is 66 lines and still fails); run `crap4dart check --all` on the tree AFTER merging main, not before. Root-cause knowledge from round 1 still stands: CI's format_coverage emits no BRDA (no --branches), CRAP is line-coverage-based; 100%-covered methods need CC≤8. CI-exact local verification: 4 shards (`dart test --total-shards 4 --shard-index N`, ~4 min each at --concurrency=2) → `lcov -a` merge → check --all + analyze; lcov and crap4dart/coverage global activations do NOT survive a runner reset — reinstall (`sudo apt-get update && apt-get install lcov`, `dart pub global activate crap4dart 0.9.5 coverage`) before the gate chain."
author: "agent"
date: "2026-09-26T18:06:12.512632Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.5
tags: ["#note", "#source_agent", "crap4dart", "coverage", "quality-gates", "gh-257", "ci", "resolved"]
---


# Note: n_0230_8987

gh-257 rework round complete (PR #259, 2026-09-26): the first fix (PR #258, already on main) and the consolidation in #259 collided — merging main back regressed `method_size` (`pathRewriteTests` 103 lines > max 60). Final shape: THREE group functions in test/pack/agent_pack_resolver_test.dart — pathRewriteTests (27 lines, basic rewrites), pathRewriteNestedTests (43, ./-prefix + nested list/map shapes), pathRewriteUntouchedTests (40, empty/plain-http/absolute/../-escape/URL/classpath pins) — all wired in main(); `_isNonPathRef` is `static`. Lesson: the 60-line method_size gate counts group-function bodies, so a 103-line function needs a 3-way split (a 2-way split of rewrite tests 1+2 alone is 66 lines and still fails); run `crap4dart check --all` on the tree AFTER merging main, not before. Root-cause knowledge from round 1 still stands: CI's format_coverage emits no BRDA (no --branches), CRAP is line-coverage-based; 100%-covered methods need CC≤8. CI-exact local verification: 4 shards (`dart test --total-shards 4 --shard-index N`, ~4 min each at --concurrency=2) → `lcov -a` merge → check --all + analyze; lcov and crap4dart/coverage global activations do NOT survive a runner reset — reinstall (`sudo apt-get update && apt-get install lcov`, `dart pub global activate crap4dart 0.9.5 coverage`) before the gate chain.

**By:** [[agent]]
**Date:** 2026-09-26T18:06:12.512632Z
**Area:** [[project|project]]
