name: dmtools
description: >-
  Pure Dart port of DMTools — enterprise dark-factory orchestrator
  (Jira, ADO, GitHub, Confluence, AI providers) with a QuickJS
  scripting runtime. See GOAL.md.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  dio: ^5.4.0
  ffi: ^2.1.0

dev_dependencies:
  test: ^1.24.0
